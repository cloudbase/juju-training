# Copyright 2021 Ubuntu
# See LICENSE file for licensing details.

name: k8s-charm
display-name: |
  My Kubernetes charm
description: |
  My Kubernetes charm description
summary: |
  My Kubernetes charm summary

containers:
  nginx:
    resource: nginx-image

resources:
  nginx-image:
    type: oci-image
    description: OCI image for nginx (nginx:stable)
    auto-fetch: true
    upstream-source: nginx:stable

requires:
  ingress:
    interface: ingress
